# 内容生成方案（基于素材爆款分析）

## 1. 目标与边界
- 目标：在现有“素材抓取 + 本地规则 + AI分析 + 图文生成 + n8n”基础上，新增“自动生成图文/视频”的完整方案。
- 目标：生成链路要显式使用素材页爆款分析结果，不是独立的随机生成。
- 目标：在左侧导航新增“内容生成”入口，作为独立业务面板。
- 边界：本文件仅提供方案，不涉及代码实现。

## 2. 现状对齐（基于当前工程）
- 素材与爆款分析已具备：`apps/frontend/src/components/materials/*`、`libraries/nestjs-libraries/src/materials/materials.analysis.service.ts`。
- 内容工厂工作流已具备：`collect -> analyze -> generate -> review -> publish`，见 `apps/orchestrator/src/workflows/content-factory.workflow.ts`。
- 图文草稿生成已具备：`ContentFactoryActivity.generateDraft(...)`。
- 视频生成能力已具备：
  - `image-text-slides`（语音+图文合成视频）
  - `veo3`（文生视频/图生视频）
  - 入口接口：`/media/video-options`、`/media/generate-video`、`/media/video/function`
- 导航当前有 `Materials / Dashboard / Tasks / Content / Insights / Generate / Publish / Logs`，见 `apps/frontend/src/components/layout/top.menu.tsx`。

## 3. 新能力定义
- 自动生成模式：
  - 单条：从某条爆款素材一键生成图文或视频。
  - 批量：按规则批量生成（如每日 TopN 爆款）。
  - 混合：同一素材同时生成“图文版 + 视频版”。
- 生成目标类型：
  - 图文（已有能力复用）
  - 视频（`image-text-slides` / `veo3`）
- 工作流目标：统一到“内容生成任务”，而非分散在多个入口。

## 4. 核心流程（建议）
1. 选源：从素材页结果中筛选爆款样本（按 `viralResult`、`scoreLayer.overallScore`、行业标签等）。
2. 组装生成简报（Generation Brief）：把分析结果转成“生成约束 + 创作指令”。
3. 路由工具：按策略决定调用图文工具或视频工具（或双路并行）。
4. 执行生成：生成草稿/脚本/视频资产并写回任务记录。
5. 质量门控：规则检查 + AI复检（标题、开场钩子、CTA、合规风险）。
6. 审核与发布：复用现有审核/发布流程，或交由 n8n 继续编排。
7. 反馈回流：发布数据回写到生成任务，形成下一轮策略优化样本。

建议编排图：

```text
素材爆款分析 -> Generation Brief -> 路由决策
                                  -> 图文生成 -> 质检 -> 审核/发布
                                  -> 视频生成 -> 质检 -> 审核/发布
                                  -> n8n扩展编排(可选)
```

## 5. 爆款分析结果如何指导生成
建议把以下字段映射为“生成指令”，避免只做参考展示：

| 分析字段 | 生成侧用途 | 典型策略 |
|---|---|---|
| `viralResult.level/score` | 决定优先级与资源投入 | `viral` 走视频+图文双路，`hot` 走图文优先 |
| `scoreLayer.dimensions` | 决定脚本补强方向 | `hook`低则强制“3秒钩子句”模板 |
| `tagLayer.hotKeywords` | 生成标题/话题词 | 标题A/B版本自动带入高频词 |
| `tagLayer.styleTags` | 决定创作风格模板 | 教程拆解/对比测评/避坑清单 |
| `timeline` 高能段 | 决定视频分镜节奏 | 高能段映射为前30秒关键镜头 |
| `summaryLayer.optimizationSuggestions` | 决定改写约束 | 把“优化建议”转为硬约束（必须含CTA等） |
| `aiDetailLayer.semantic.profile360` | 决定人设与表达方式 | 口播语气、权威背书、风险规避策略 |

## 6. 工具集成与逻辑关系

### 6.1 工具层抽象（建议）
- `BriefBuilder`：分析结果 -> 生成简报。
- `TextImageGenerator`：复用当前图文草稿能力。
- `VideoGeneratorSlides`：调用 `image-text-slides` 生成说明型/口播型视频。
- `VideoGeneratorVeo3`：调用 `veo3` 生成创意型视频。
- `QualityGate`：统一做结构检查、风险检查、素材可用性检查。
- `DeliveryConnector`：发布到现有发布链路或推送 n8n。

### 6.2 路由策略（建议）
- 场景A（效率优先）：先出图文，再按需要补视频。
- 场景B（复盘优先）：基于高能时间轴先出短视频脚本，直接走视频工具。
- 场景C（批量运营）：n8n 定时触发，批量生成并分发审核任务。

路由示例：

| 条件 | 默认路由 |
|---|---|
| `viral>=3` 且有清晰视觉线索 | `veo3` + 图文并行 |
| 信息密度高、教程类标签 | `image-text-slides` + 图文摘要 |
| 素材媒体缺失或质量低 | 仅图文生成，视频延后 |
| 高风险行业词命中 | 先图文草稿+人工审核，视频需二次确认 |

## 7. 左侧导航与页面方案

### 7.1 导航改造
- 在左侧导航新增按钮：`内容生成`（建议英文 key：`factory_creation`）。
- 建议位置：`Insights` 与 `Generate` 之间，强调“分析到生成”的承接关系。
- 建议路由：`/creation`（或 `/content-generation`，二选一统一）。

### 7.2 页面结构（内容生成页）
- 模块1：爆款来源选择（素材池、过滤条件、TopN 规则）。
- 模块2：生成策略配置（目标平台、图文/视频模式、品牌语气、合规级别）。
- 模块3：工具与模板配置（图文模板、视频工具、n8n流程模板）。
- 模块4：任务队列（运行中/成功/失败/待审核）。
- 模块5：结果资产区（草稿、脚本、视频、封面、回放链接）。

### 7.3 与现有页面关系
- `Materials/Insights`：负责“发现什么值得做”。
- `内容生成`：负责“把值得做的变成可发布内容”。
- `Generate/Publish`：可逐步收敛到内容生成页，或保留为高级操作入口。

## 8. n8n 集成方案（方案级）
- 触发方式：
  - 定时触发：每天固定时间拉取爆款TopN并自动生成。
  - 事件触发：素材分析完成且达到阈值时触发生成。
- 编排方式：
  - `Postiz -> n8n`：通过 webhook 推送 `generation.created / generation.completed / generation.failed`。
  - `n8n -> Postiz`：调用生成启动API，传策略参数与幂等键。
- 关键控制：
  - 幂等键：避免同一素材重复生成。
  - 重试与告警：失败任务进入重试队列并通知。
  - 回写：n8n最终执行结果回写生成任务状态。

## 9. 数据与任务模型建议（方案级）
- `generation_task`：任务主表（sourceContentIds、mode、strategy、status、traceId）。
- `generation_brief`：由分析结果生成的约束快照（防止分析更新导致任务漂移）。
- `generation_asset`：产物表（draft/script/video/cover 等）。
- `generation_execution`：各工具执行日志（tool、latency、cost、error）。
- `generation_feedback`：发布后表现（曝光、互动、转化）用于策略回流。

## 10. 分阶段落地计划
1. Phase 1（最小闭环）
   - 新增“内容生成”页面与左侧入口。
   - 打通“爆款样本 -> 图文自动生成 -> 任务可追踪”。
2. Phase 2（视频增强）
   - 接入 `image-text-slides` 与 `veo3` 自动路由。
   - 增加质量门控与失败降级（视频失败自动回退图文）。
3. Phase 3（自动化运营）
   - 接入 n8n 批量编排、定时策略、回写闭环。
   - 上线策略看板与效果归因。

## 11. 验收标准（建议）
- 业务闭环：能从爆款素材一键生成图文/视频，并进入审核/发布。
- 自动化能力：支持按规则批量自动生成，支持失败重试与告警。
- 可观测性：任务状态、工具调用、失败原因、耗时成本可追踪。
- 质量指标：生成通过率、平均生成时长、人工改稿率逐步下降。

## 12. 关键风险与对策
- 风险：视频生成成本高、延迟高。
  - 对策：默认图文优先，视频按评分阈值触发。
- 风险：素材质量不足导致视频失败。
  - 对策：生成前做素材完整性检查，失败自动降级图文。
- 风险：批量自动生成质量波动大。
  - 对策：加入模板白名单、行业黑名单、人工抽检位。

